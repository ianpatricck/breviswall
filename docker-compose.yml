services:
  app:
    build: .
    container_name: easy-kanban-app
    ports:
      - "${APP_PORT}:${APP_PORT}"
    volumes:
      - .:/usr/src/easy-kanban
    working_dir: /usr/src/easy-kanban
    command: sh -c "composer install && composer api:generate && php -S ${APP_DOCKER_URL} -t public"
